{
    "builds": [{
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
            "maxLambdaSize": "50mb"
        }
    }],
    "scripts": {
        "build": "npm install && npx vite build"
    }
}